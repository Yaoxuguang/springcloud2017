### bootstrap 上下文配置
### 配置spring cloud config 服务器的uri
spring:
  cloud:
    config:
      uri: http://localhost:9090
      # profile 是激活配置
      profile: test
      # label 在Git中指的分支名称
      label: master
      # 配置客户端应用名称:{application}
      name: yao

